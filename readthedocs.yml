version: 2
formats:
  - htmlzip

# build with latest available ubuntu version
build:
  os: ubuntu-20.04
  tools:
    python: "3.9"
  jobs:
    post_install:
      - playwright install

python:
  install:
    - method: pip
      path: .[doc]
